/**
 * Design Tokens Single Source of Truth Re-export
 *
 * Components can import colors directly:
 * `import { brand } from '@/lib/tokens';`
 * `brand.navy` -> "#0A1B39"
 * `brand.teal` -> "#0BA5EC"
 */

export {
  brand,
  brandTokens,
  grayTokens,
  semanticTokens,
  badgeTokens,
  designTokens,
} from "../../tailwind.config";
